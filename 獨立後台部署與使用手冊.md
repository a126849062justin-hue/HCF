# HCF 獨立後台｜部署與使用手冊

你自己的後台：數據進自己的資料庫、自己的網址登入，不經過任何第三方分析平台。

## 一、後台能做什麼
| 分頁 | 功能 |
|---|---|
| 儀表板 | 即時 KPI（瀏覽/預約點擊/LINE/問卷名單/AI 提問/社群）、每日趨勢圖、轉換漏斗、五大目標點擊圓餅、熱門頁面、AI 最常被問問題 |
| 名單 | 問卷送出自動進名單；狀態流轉（新名單→已聯絡→已成交/無效）、備註、一鍵下載 CSV |
| 內容管理 | ①全站公告列開關 ②最新課表格子編輯（存檔官網立即更新）③每月公告（最新資訊頁）④價格（課程方案 15 列＋補給箱 3 格） |
| 系統 | 一鍵下載完整備份 JSON、上傳還原（換主機無痛搬家）；檔案模式另有每日自動快照保留 7 份 |

## 二、五分鐘本機驗收
1. 電腦裝 Node.js 18+（nodejs.org 下載安裝）
2. 進 backend 資料夾，執行：`node server.js`
3. 瀏覽器開 http://localhost:8088 → 密碼 `hcf-change-me` 登入
資料存在 backend/data/，不用安裝任何套件。

## 三、正式上雲（最省事免費組合：Render ＋ Neon）
查證過的現況：Render 免費版程式會在閒置 15 分鐘後休眠、且不能掛持久磁碟，所以資料要放免費的 Neon Postgres 才不會丟。

**步驟 A｜建資料庫（Neon）**
1. neon.tech 註冊 → Create Project（區域選 Singapore 最近）
2. 複製 Connection string（postgres://… 開頭整串）

**步驟 B｜部署後台（Render）**
1. 把 backend 資料夾推上你的 GitHub（或建新 repo 只放這個資料夾）
2. render.com 註冊 → New → Web Service → 連 GitHub 選該 repo
3. 設定：Build Command `npm install`、Start Command `node server.js`、Instance type 選 Free
4. Environment 加三個變數：
   - `ADMIN_PASSWORD` ＝ 你的後台密碼（務必自訂）
   - `DATABASE_URL` ＝ 步驟 A 的連線字串
   - `SITE_ORIGIN` ＝ https://你的官網網域（選填，加了更安全）
5. 部署完成會得到網址，例如 `https://hcf-admin.onrender.com` → 開啟即是後台登入頁

**步驟 C｜官網對接（一個欄位）**
1. 打開官網 `site/hcf-analytics.js`，把第 10 行 `var BACKEND='';` 填入後台網址
2. 官網重新部署（拖 Netlify）→ 完成。訪客事件開始進你的後台，內容管理即時生效

## 四、已知取捨與升級路
- Render 免費版休眠：你打開後台第一下要等 30–60 秒喚醒；訪客瀏覽事件也會把它喚醒，喚醒前幾秒的事件會漏記。介意的話 Render 升 $7/月常駐即解
- GA4 可同時並用：hcf-analytics.js 的 GA_ID 與 BACKEND 互不衝突，想交叉比對就兩個都填
- 想搬家：系統分頁「下載完整備份」→ 新主機「上傳還原」

## 五、安全須知
- 密碼錯 5 次鎖 10 分鐘；登入憑證 7 天效期、HttpOnly Cookie
- 公開 API 只開放：收事件（白名單事件名）、收名單、讀內容；所有管理操作都要登入
- 千萬不要用預設密碼上線（後台會用紅字提醒你）
